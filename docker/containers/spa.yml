spa:
  container_name: spa
  image: spa
  environment:
    - MONGO_URL=mongodb://172.30.2.160:27017/promed
    - ROOT_URL=http://54.174.42.80
    - PORT=3000
    - TERM=xterm
  ports:
    - "3000:3000"
  restart: always
  volumes:
    - /shared:/shared
  command: supervisord --nodaemon --config /etc/supervisor/supervisord.conf
promed-scraper:
  container_name: promed-scraper
  image: promed-scraper
  environment:
    - MONGO_URL=172.30.2.160:27017
    - SPARQLDB_URL=http://niam.eha.io:8890/sparql-auth?default-graph-uri=http://eha.io/t11
    - TERM=xterm
    - GOOGLE_TRANSLATE_KEY=AIzaSyAYY0raBaOLjdFGpe2A3NZ3O9CaYgYfQ-M
  restart: always
  volumes:
    - /shared:/shared
